// src/environments/environment.prod.ts
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8000/api' // <-- CAMBIA ESTO por la URL de tu API en producción (ej. 'https://api.tudominio.com')
};